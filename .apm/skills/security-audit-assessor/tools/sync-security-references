#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"$script_dir/build-audit-corpus"
"$script_dir/build-audit-index"
"$script_dir/validate-security-corpus"
