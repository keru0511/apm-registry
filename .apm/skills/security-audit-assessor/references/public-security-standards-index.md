# 公的セキュリティ基準インデックス

このファイルは後方互換用の案内だけを残す。
主索引は次に移した。

- `index/security-standards.json`
- `index/README.md`
- `mappings/system-controls.json`
- `mappings/governance-controls.json`

通常は Markdown 一覧ではなく、構造化された JSON 索引と mapping を使う。
原文はローカル保持せず、必要時だけ `security-standards.json` の `canonical_url` から公式サイトを参照する。
