#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
tool_path="$repo_root/.apm/skills/security-audit-assessor/tools/validate-security-corpus"

exec "$tool_path" "$@"
